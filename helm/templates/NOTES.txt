╔══════════════════════════════════════════════════════════════╗
║          🛡️  INTERLOCK DEPLOYED SUCCESSFULLY  🛡️           ║
╚══════════════════════════════════════════════════════════════╝

Release: {{ .Release.Name }}
Namespace: {{ .Release.Namespace }}
Chart Version: {{ .Chart.Version }}
App Version: {{ .Chart.AppVersion }}

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

🌐 ACCESS INTERLOCK:

{{- if .Values.ingress.enabled }}
{{- range .Values.ingress.hosts }}
  Public URL: https://{{ .host }}
{{- end }}
{{- else }}
  Run this to port-forward:
    kubectl port-forward -n {{ .Release.Namespace }} svc/{{ include "interlock.fullname" . }} 8001:80
  Then visit: http://localhost:8001/docs
{{- end }}

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

🚀 QUICK TEST:

  # Mint an API key first: POST /admin/tokens -> POST /admin/keys (needs ADMIN_TOKEN).
  curl -X POST https://api.interlock.example.com/v1/chat/completions \
    -H "x-api-key: YOUR_API_KEY_HERE" \
    -H "Content-Type: application/json" \
    -d '{
      "model": "gpt-4o",
      "messages": [{"role": "user", "content": "Hello!"}]
    }'

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

📊 MONITORING:

  View pods:        kubectl get pods -n {{ .Release.Namespace }} -l app.kubernetes.io/name={{ include "interlock.name" . }}
  View logs:        kubectl logs -n {{ .Release.Namespace }} -l app.kubernetes.io/name={{ include "interlock.name" . }} -f
  View HPA:         kubectl get hpa -n {{ .Release.Namespace }}
  Health check:     kubectl exec -n {{ .Release.Namespace }} deploy/{{ include "interlock.fullname" . }} -- curl localhost:8001/health

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

🔐 NEXT STEPS:

  1. Configure upstream LLM API keys:
     kubectl edit secret {{ include "interlock.fullname" . }}-secrets -n {{ .Release.Namespace }}

  2. Set up custom domain & TLS:
     Update values.yaml -> ingress.hosts

  3. Enable Prometheus monitoring:
     Set values.yaml -> metrics.serviceMonitor.enabled=true

  4. View dashboard:
     Open https://api.interlock.example.com/dashboard.html

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

📚 DOCUMENTATION: https://github.com/MaazAhmed47/Interlock
💬 SUPPORT:       maazahmed1856@gmail.com
🔒 SECURITY:      maazahmed1856@gmail.com

