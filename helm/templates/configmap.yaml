apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "interlock.fullname" . }}-config
  labels:
    {{- include "interlock.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.config }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
